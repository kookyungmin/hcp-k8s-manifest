apiVersion: v1
kind: Service
metadata:
  name: hcp-compute-service-svc
  namespace: hcp
  labels:
    app: hcp-compute-service
spec:
  selector:
    app: hcp-compute-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP