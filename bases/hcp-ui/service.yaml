apiVersion: v1
kind: Service
metadata:
  name: hcp-ui-svc
  namespace: hcp
  labels:
    app: hcp-ui
spec:
  selector:
    app: hcp-ui
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP