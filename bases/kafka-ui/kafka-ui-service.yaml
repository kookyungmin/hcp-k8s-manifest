apiVersion: v1
kind: Service
metadata:
  name: hcp-kafka-ui-svc
  namespace: hcp
spec:
  selector:
    app: hcp-kafka-ui
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
