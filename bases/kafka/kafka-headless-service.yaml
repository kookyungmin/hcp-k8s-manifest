apiVersion: v1
kind: Service
metadata:
  name: hcp-kafka-headless-svc
  namespace: hcp
spec:
  clusterIP: None
  selector:
    app: hcp-kafka
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093
