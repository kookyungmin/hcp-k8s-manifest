apiVersion: apps/v1
kind: Deployment
metadata:
  name: hcp-orchestrator-worker
  namespace: hcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hcp-orchestrator-worker
  template:
    metadata:
      labels:
        app: hcp-orchestrator-worker
    spec:
      serviceAccountName: hcp-orchestrator-worker
      containers:
        - name: hcp-orchestrator-worker
          image: rudals4549/hcp-orchestrator-worker:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: hcp-orchestrator-worker-config
            - secretRef:
                name: hcp-orchestrator-worker-secret
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=50 -XX:InitialRAMPercentage=20"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 10
            periodSeconds: 15
            failureThreshold: 10

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 10
            periodSeconds: 15
            failureThreshold: 10
