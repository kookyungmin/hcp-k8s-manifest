apiVersion: v1
kind: Service
metadata:
  name: hcp-orchestrator-worker-svc
  namespace: hcp
spec:
  selector:
    app: hcp-orchestrator-worker
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP