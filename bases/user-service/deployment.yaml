apiVersion: apps/v1
kind: Deployment
metadata:
  name: hcp-user-service
  namespace: hcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hcp-user-service
  template:
    metadata:
      labels:
        app: hcp-user-service
    spec:
      containers:
        - name: hcp-user-service
          image: rudals4549/hcp-user-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: hcp-user-service-config
            - secretRef:
                name: hcp-user-service-secret

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 5

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 5
