apiVersion: v1
kind: Service
metadata:
  name: hcp-user-service-svc
  namespace: hcp
  labels:
    app: hcp-user-service
spec:
  selector:
    app: hcp-user-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP